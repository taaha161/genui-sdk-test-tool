name: genui_devtools
description: DevTools extension for the GenUI SDK Surface Simulator.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.27.1"

dependencies:
  flutter:
    sdk: flutter
  genui: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.0
  devtools_extensions: ^0.5.0

flutter:
  uses-material-design: true
